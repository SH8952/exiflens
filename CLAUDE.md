@AGENTS.md


## 백업 위치 규칙 (절대 준수)

이 프로젝트(`exiflens`) 작업 전 백업을 생성할 때는 반드시 **이 폴더 내부**에 만든다: `_backups/exiflens_backup_YYYYMMDD_HHMMSS`
상위 폴더(`Desktop/애드센스 제휴 마케팅/`)에 직접 백업 폴더를 만들지 않는다 (예: `../exiflens_backup_...`, `../_backups/exiflens_backup_...` 전부 금지).
